-- ============================================================
-- sermons.image_urls : 설교안 게시판에서 스크랩한 원문 이미지(파싱 순서 그대로)
-- 가교모임 - 설교요약 탭에서 차례대로 보여주기 위함.
-- ============================================================
alter table sermons
  add column image_urls jsonb not null default '[]'::jsonb;
